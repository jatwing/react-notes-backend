{
  "name": "react-notes-backend",
  "version": "1.0.0",
  "type": "commonjs",
  "engines": {
    "node": ">=8.10.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongodb": "^4.1.4",
    "serverless-http": "^2.7.0"
  },
  "scripts": {
    "start": "nodemon src/app.js",
    "debug": "serverless deploy --stage staging",
    "deploy": "serverless deploy --stage production",
    "inform": "serverless info --stage staging && serverless info --stage production",
    "format": "yarn prettier --write .",
    "lint": "yarn eslint . --fix"
  },
  "devDependencies": {
    "eslint": "^8.2.0",
    "eslint-plugin-node": "^11.1.0",
    "nodemon": "^2.0.15",
    "prettier": "^2.4.1",
    "serverless": "^2.66.1"
  }
}
